services:
  opencode:
    # No portal: this server is unauthenticated and can access files and tools.
    command: .agents/opencode serve --hostname 127.0.0.1 --port "$PORT"
    port: 4096
    health: /global/health
